name: Executing via script 

on: push

jobs:
  shell-script:
     runs-on: ubuntu-latest
     steps:
     - name: Checkout
       uses: actions/checkout@v4

     - name: Exec shell script
       run: ./myscript.sh

